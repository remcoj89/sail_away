<div class="container">
  <h1>All of our boats</h1>
  <% @boats.each do |boat| %>
    <div class="row row-cols-1 row-cols-md-2 g-4">
      <div class="col">
          <div class="card" style="width: 18rem;">
            <div class="card-image"
                style="background-image: url('<%= cl_image_path boat.photo.key%>')">
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= boat.name %></h5>
              <p class="card-text"><%= boat.price %></p>
              <%= link_to "More info", boat_path(boat), class: "btn btn-primary" %>
              <%= link_to "Delete", boat_path(boat), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-warning" %>
            </div>
          </div>
      </div>
    </div>
  <% end %>
</div>
<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>




<%= link_to "rent out a new boat",  new_boat_path %>
